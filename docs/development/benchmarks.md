# Benchmarks

_Documentation for this page is in progress._

<!-- TODO(eng): owners Ekin / Dan F. Write the Benchmarks page. Suggested coverage:
     - What is measured: cold start vs warm (restore) start; metric = time to pod ready.
     - Method: same model/GPU/node; checkpoint once then restore; record storage backend
       and same-node vs cross-node restore; multiple trials, report the distribution.
     - How to reproduce: the benchmark harness and exact commands.
     - Results: table (model | GPU | storage | cold start | restore) with measured
       values only. Do not publish target or aspirational numbers. -->
